import React from 'react';
import {MoviesList} from "../components";

const PageMovies = () => {
    return (
        <div>
            <MoviesList/>
        </div>
    );
};

export {
    PageMovies
};